# ========================
# 必需配置 (Required)
# ========================

# 应用基本信息
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Nano Banana Ultra

# 主题配置
NEXT_PUBLIC_THEME=default
NEXT_PUBLIC_APPEARANCE=system

# 数据库
# PostgreSQL (Supabase): postgresql://user:password@host:port/database
# Cloudflare D1: 通过 wrangler.toml 配置
DATABASE_URL=
DATABASE_PROVIDER=turso
DB_SINGLETON_ENABLED=true

# 认证密钥 (运行 openssl rand -base64 32 生成)
AUTH_SECRET=

# ========================
# Cloudflare 部署配置 (Cloudflare Workers 部署时需要)
# ========================

# Cloudflare Account ID (从 Cloudflare Dashboard 获取)
# CLOUDFLARE_ACCOUNT_ID=

# KV Namespace ID (运行 wrangler kv:namespace create SESSION_KV 后获取)
# CLOUDFLARE_KV_NAMESPACE_ID=

# KV API Token (用于本地开发时访问 KV，可选)
# CLOUDFLARE_KV_API_TOKEN=

# ========================
# 可选配置 (Optional)
# ========================

# Sentry 错误监控（留空则禁用）
# 从 https://sentry.io 获取
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
# 采样率（默认生产环境 1%，开发环境 10%）
SENTRY_TRACES_SAMPLE_RATE=
NEXT_PUBLIC_SENTRY_TRACES_SAMPLE_RATE=

# ========================
# 注意事项
# ========================
#
# 以下配置通过管理后台 /admin/settings 配置，存储在数据库 config 表中：
#
# - AI 提供商: gemini_api_key, replicate_api_token, fal_api_key, kie_api_key
# - 支付提供商: stripe_*, creem_*, paypal_*
# - 存储: r2_access_key, r2_secret_key, r2_bucket_name, r2_domain
# - 邮件: resend_api_key, resend_sender_email
# - 认证: google_client_id, google_client_secret, github_client_id, github_client_secret
# - 分析: google_analytics_id, clarity_id, plausible_domain
# - 客服: crisp_website_id, tawk_property_id
#
# 这些配置不需要放在 .env 文件中！
