# {Feature} UIUX Design Specification

> Generated: {TIMESTAMP}
> Author: Claude UIUX Developer
> Version: 1.0

---

## 1. Design Goal

{From intent analysis - describe the user's goal and what this design aims to achieve}

---

## 2. Component Specification

| Scene | Component | Variant | Props | Notes |
|-------|-----------|---------|-------|-------|
| {Scene description} | `Button` | `glow-primary` | `size="lg"` | Primary CTA |
| {Scene description} | `Card` | `interactive` | - | Hover enabled |
| {Scene description} | `Input` | `neon` | `placeholder="..."` | Hero input |

### Component Details

#### 2.1 {Component Name}

**Purpose**: {What this component does}

**Variant**: `{variant}` (from CBDS)

**Props**:
```tsx
{
  size: "lg",
  variant: "glow-primary",
  loading: false
}
```

**Source**: `.agent/rules/UIUX_Guidelines.md` Line {N}

---

## 3. Interaction Specification

| Interaction | Trigger | Effect | Duration | Easing |
|-------------|---------|--------|----------|--------|
| Button hover | `mouseenter` | Glow 20→30px + scale(1.02) | 200ms | ease-out |
| Card hover | `mouseenter` | Lift + border glow | 300ms | ease-out |
| Page enter | `mount` | Fade in + slide up | 500ms | ease-out |

### State Definitions

#### 3.1 Loading State
- **Trigger**: Form submit / API call
- **Visual**: `<Loader2 className="animate-spin text-primary" />`
- **Behavior**: Disable all inputs, show skeleton

#### 3.2 Error State
- **Trigger**: Validation fail / API error
- **Visual**: Red border + error message below
- **Behavior**: Focus first error field

#### 3.3 Empty State
- **Trigger**: No data available
- **Visual**: Illustrated empty state with CTA
- **Copy**: "{Encouraging message}"

---

## 4. Responsive Specification

| Breakpoint | Width | Layout Changes |
|------------|-------|----------------|
| Desktop | >= 1024px | Full layout, hover effects |
| Tablet | 768-1023px | {Specific changes} |
| Mobile | < 768px | {Specific changes} |

### Mobile-Specific Requirements

- [ ] Touch targets >= 44px
- [ ] Critical actions in thumb zone
- [ ] Horizontal scroll for gallery (max 6 cards)
- [ ] Simplified blur effects

### Viewport-Specific Mockups

**Desktop (1280px)**:
```
┌─────────────────────────────────────────────┐
│  {ASCII layout representation}              │
└─────────────────────────────────────────────┘
```

**Mobile (375px)**:
```
┌─────────────┐
│  {ASCII}    │
└─────────────┘
```

---

## 5. Animation Specification

| Element | Animation | Parameters | Trigger |
|---------|-----------|------------|---------|
| Hero section | Fade in | `opacity: 0→1, duration: 0.5s` | Page load |
| Cards | Stagger | `delay: index * 0.1s` | Viewport enter |
| CTA button | Shimmer | `infinite, 3s` | Always |

### Motion Preferences

```css
@media (prefers-reduced-motion: reduce) {
  /* Disable non-essential animations */
  .shimmer-effect { animation: none; }
  .hover-scale { transform: none; }
}
```

---

## 6. Accessibility Requirements

| Requirement | Implementation |
|-------------|----------------|
| Focus visible | Ring-2 primary with offset |
| Screen reader | aria-label on icon buttons |
| Keyboard nav | Tab order follows visual |
| Color contrast | WCAG AA (4.5:1) |

---

## 7. CBDS Compliance Checklist

- [ ] No forbidden patterns (hardcoded colors, wrong variants)
- [ ] All components use CBDS variants
- [ ] Hover effects follow spec
- [ ] Loading states implemented
- [ ] Touch targets >= 44px on mobile
- [ ] Animations respect reduced-motion

---

## 8. Implementation Notes

### Dependencies
- `framer-motion` for animations
- `lucide-react` for icons
- `@/shared/components/ui/*` for CBDS components

### Key Files to Modify
1. `{file_path}` - Main component
2. `{file_path}` - Types/interfaces
3. `{file_path}` - Locale strings

### Estimated Scope
- Files to create: {N}
- Files to modify: {N}
- New components: {N}

---

## Self-Review Report

| Dimension | Status | Notes |
|-----------|--------|-------|
| Completeness | ✅ | All sections filled |
| CBDS Reference | ✅ | Line numbers cited |
| Responsive | ✅ | All breakpoints covered |
| Accessibility | ✅ | WCAG requirements listed |
| Animations | ✅ | Motion preferences included |

---

*Generated by UIUX Developer Skill v1.0*
