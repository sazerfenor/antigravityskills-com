---
description: CPO Builder - 汇总多方专业建议生成可落地 PRP
argument-hint: [Feature_Spec.md path or feature description]
---

# CPO Builder Workflow

> **核心定位**: CPO 汇总器 - 只接收汇总，不执行具体调研
> **关键原则**: 决策需与用户确认，不自作主张精简

## 🔁 Handover Convention

| 方向 | 内容 |
|------|------|
| **必选输入** | `/0-pm-agent` 输出目录 (含 Feature_Spec.md + 过程文件) |
| **推荐输入** | 代码考古报告 + 漏洞分析报告 + 用户心理报告 |
| **可选输入** | `UIUX_Spec.md`, `SEO_Spec.md`, `Prompt_Spec.md` |
| **输出** | `docs/prps/{feature-name}.md` + `docs/tasks/{feature-name}.md` |
| **下游** | `/1-persona-review` (用户视角评审) |

> [!IMPORTANT]
> **必须读取本次会话的聊天上下文**：在汇总决策时，除了读取文件，还要回顾与用户/上游 Agent 的对话，提取决策过程、用户反馈、被否决的方案。

---

## 📚 Agent Roster

| Agent | 文件路径 | 阶段 |
|-------|---------|------|
| **preflight-prp** | [preflight-prp-enhanced.md](cpo-builder/preflight-prp-enhanced.md) | Phase 0: 输入验证 |
| **codebase-research** | [codebase-research.md](cpo-builder/codebase-research.md) | Phase 1: 约束扫描 |
| **cpo-decision** | [cpo-decision.md](cpo-builder/cpo-decision.md) | Phase 2: CPO 决策 |
| **team-lead-task-breakdown** | [team-lead-task-breakdown.md](cpo-builder/team-lead-task-breakdown.md) | Phase 3: 任务拆解 |

---

## 📥 输入来源矩阵

| 来源 | 必选 | 输入文件 | 负责工作流 |
|------|------|---------|-----------|
| 产品策略 | ✅ 必选 | `Feature_Spec.md` | `/0-pm-agent` |
| 技术约束 | ⭐ 推荐 | `代码考古报告.md` | `/0-pm-agent` |
| 风险清单 | ⭐ 推荐 | `漏洞分析报告.md` | `/0-pm-agent` |
| 用户洞察 | ⭐ 推荐 | `用户心理报告.md` | `/0-pm-agent` |
| UIUX 设计 | 可选 | `UIUX_Spec.md` | `/0-uiux-designer` |
| Prompt 优化 | 可选 | `Prompt_Spec.md` | `/0-prompt-gen` |
| SEO 建议 | 可选 | `SEO_Spec.md` | 🔮 待建 |

---

## 📋 Phase 0: 输入接收与验证

Call /preflight-prp

### Step 0.1: 检查必选输入

**执行**:
1. 检查 `Feature_Spec.md` 是否存在
2. 如不存在，提示用户先运行 `/0-pm-agent`

**GATE**:
- ❌ REJECT: 无 Feature_Spec.md
- ✅ PASS: 继续

### Step 0.2: 收集可选输入

**执行**:
1. 询问用户是否有其他专业建议文档
2. 收集并列出所有输入源

**输出示例**:
```
📥 收到的输入:
✅ Feature_Spec.md (必选)
✅ UIUX_Spec.md (可选)
❌ SEO_Spec.md (未提供)
❌ Prompt_Spec.md (未提供)
```

### ⏸️ CHECKPOINT 0
> **检查点**: 确认所有输入已收集
> **回复**: "继续" 或补充更多输入

---

## 📋 Phase 1: 代码库约束扫描

Call /codebase-research

### Step 1.1: 技术约束提取

**执行**:
1. 快速扫描代码库，提取现有模式
2. 识别技术约束和可复用资源
3. **不做外部调研** (只扫描内部代码)

**Output**: 约束摘要 (供 Phase 2 使用)

---

## 📋 Phase 2: CPO 决策与汇总

Call /cpo-decision

> [!IMPORTANT]
> **核心原则**: 所有决策必须与用户确认，不自作主张

### Step 2.1: 生成 PRD-ADR 决策记录

**执行**:
1. 读取所有输入文档
2. 提取功能点 → Decision Drivers 分析
3. MoSCoW 分类 → RICE 评分
4. 生成 PRD-ADR 决策记录

### Step 2.2: CPO 决策确认 (MANDATORY)

**对于每个决策点，必须询问用户**:

```markdown
## 🎯 CPO 决策确认

### 决策 1: [功能取舍]
**来源**: Feature_Spec vs UIUX_Spec 冲突
**选项**:
- A) 采用 Feature_Spec 建议: [具体内容]
- B) 采用 UIUX_Spec 建议: [具体内容]
- C) 合并方案: [具体内容]

请选择 (A/B/C):

### 决策 2: [优先级排序]
...
```

### ⏸️ CHECKPOINT 2 (不可跳过)
> **检查点**: 所有决策已确认
> **回复**: 对每个决策点做出选择

---

## 📋 Phase 3: PRP 生成

**模板**: 使用 `.agent/doc/templates/prp_document_template.md`

### Step 3.1: 生成 PRP 文档

**执行**:
1. 根据确认的决策生成 PRP
2. 包含所有已确认的功能规格
3. 明确标注每个规格的来源

**Output**: `docs/prps/{feature-name}.md`

### Step 3.2: 任务拆解

Call /team-lead-task-breakdown

**执行**:
1. 将 PRP 拆解为开发任务
2. 定义任务依赖关系
3. 使用 `.agent/doc/templates/technical-task-template.md`

**Output**: `docs/tasks/{feature-name}.md`

---

## ✅ 最终输出

| 文件 | 路径 | 用途 |
|------|------|------|
| PRP 文档 | `docs/prps/{feature-name}.md` | 下游评审 |
| 任务清单 | `docs/tasks/{feature-name}.md` | 开发执行 |

**下游**: `/1-prd-review` (用户视角评审)

---

## Quality Checklist

- [ ] Feature_Spec.md 已读取 (必选)
- [ ] 所有可选输入已收集
- [ ] 代码库约束已扫描
- [ ] **所有决策已与用户确认**
- [ ] PRP 文档已生成
- [ ] 任务已拆解

---

## 语言规范

- **沟通阶段**: 使用用户的语言
- **最终 PRP**: 英文输出
- **代码示例**: 英文变量名

---

**Version**: 2.1 | **Created**: 2025-12-21 | **Updated**: 2025-12-25 - 添加 CPO Decision Agent (ADR+MoSCoW+RICE)
