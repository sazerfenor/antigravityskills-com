@layer base {
  :root {
    /* 
     * NANOBANANA LIGHT THEME
     * Strategy: High-Key White/Cream base with Banana Yellow Accents
     */
    
    /* BACKGROUND: Pure White for clarity */
    --background: oklch(1 0 0);
    
    /* FOREGROUND: Deep Slate (Not pure black) for softer reading */
    --foreground: oklch(0.141 0.005 285.8); 

    /* CARDS: White surface with subtle borders */
    --card: oklch(1 0 0);
    --card-foreground: oklch(0.141 0.005 285.8);
    --popover: oklch(1 0 0);
    --popover-foreground: oklch(0.141 0.005 285.8);

    /* 
     * PRIMARY: BANANA YELLOW
     * Hex #ffe135 -> OKLCH(0.89 0.18 95)
     * High lightness, vibrant chroma.
     */
    --primary: oklch(0.89 0.18 95);

    /* 
     * PRIMARY FOREGROUND: CRITICAL INVERSION
     * Must be dark to contrast with the light yellow button.
     * Using the Deep Slate color ensures harmony with the text.
     */
    --primary-foreground: oklch(0.141 0.005 285.8);

    /* 
     * SECONDARY: SOFT CREAM
     * A very light yellow-grey for secondary buttons.
     * Hints at the "banana flesh" color.
     */
    --secondary: oklch(0.97 0.02 95);
    --secondary-foreground: oklch(0.141 0.005 285.8);

    /* MUTED: Neutral greys for structure */
    --muted: oklch(0.96 0.01 285);
    --muted-foreground: oklch(0.55 0.02 285);

    /* ACCENT: Hover states */
    --accent: oklch(0.93 0.12 95); /* A slightly darker yellow for hover */
    --accent-foreground: oklch(0.141 0.005 285.8);

    /* DESTRUCTIVE: Standard Red */
    --destructive: oklch(0.6 0.2 25);
    --destructive-foreground: oklch(0.98 0 0);

    /* BORDERS & INPUTS */
    --border: oklch(0.92 0.01 285);
    --input: oklch(0.92 0.01 285);

    /* RING: The "Banana Glow" Focus Ring */
    --ring: oklch(0.89 0.18 95);

    /* CHARTS: Adapted to harmonize with Yellow */
    --chart-1: oklch(0.89 0.18 95); /* Yellow (Brand) */
    --chart-2: oklch(0.141 0.005 285.8); /* Black/Slate (Contrast) */
    --chart-3: oklch(0.6 0.15 250); /* Blue (Trust) */
    --chart-4: oklch(0.7 0.15 45); /* Orange (Analogous) */
    --chart-5: oklch(0.6 0.15 150); /* Green (Success) */
    
    /* RADIUS: Increased radius for "Organic/Banana" feel */
    --radius: 0.75rem; 

    /* SHADOWS: Tinted slightly yellow for warmth */
    --shadow-sm: 0 1px 2px 0 rgb(255 225 53 / 0.05);
    --shadow: 0 1px 3px 0 rgb(255 225 53 / 0.1), 0 1px 2px -1px rgb(255 225 53 / 0.1);
    --shadow-md: 0 4px 6px -1px rgb(255 225 53 / 0.1), 0 2px 4px -2px rgb(255 225 53 / 0.1);
  }

 .dark {
    /* 
     * NANOBANANA DARK THEME
     * Strategy: "Cyberpunk Lab" - Obsidian Base with Neon Yellow
     */
    
    /* BACKGROUND: Deep Obsidian (Blue-tinted Black) */
    --background: oklch(0.14 0.02 280); 
    --foreground: oklch(0.98 0 0);

    /* CARDS: Slightly lighter obsidian */
    --card: oklch(0.17 0.02 280);
    --card-foreground: oklch(0.98 0 0);
    --popover: oklch(0.17 0.02 280);
    --popover-foreground: oklch(0.98 0 0);

    /* PRIMARY: Neon Banana Yellow */
    --primary: oklch(0.89 0.18 95);
    --primary-foreground: oklch(0.141 0.005 285.8); /* Dark text persists */

    /* SECONDARY: Dark Slate */
    --secondary: oklch(0.25 0.02 280);
    --secondary-foreground: oklch(0.98 0 0);

    /* MUTED */
    --muted: oklch(0.25 0.02 280);
    --muted-foreground: oklch(0.7 0.01 280);

    /* ACCENT */
    --accent: oklch(0.25 0.02 280);
    --accent-foreground: oklch(0.98 0 0);

    /* DESTRUCTIVE */
    --destructive: oklch(0.6 0.2 25);
    --destructive-foreground: oklch(0.98 0 0);

    /* BORDERS: Subtle separation */
    --border: oklch(0.25 0.02 280);
    --input: oklch(0.25 0.02 280);

    /* RING */
    --ring: oklch(0.89 0.18 95);
    
    /* Chart Colors adjusted for dark mode visibility */
    --chart-1: oklch(0.89 0.18 95);
    --chart-2: oklch(0.6 0.15 250);
    --chart-3: oklch(0.7 0.15 45);
    --chart-4: oklch(0.6 0.15 150);
    --chart-5: oklch(0.8 0.1 330);
  }
}